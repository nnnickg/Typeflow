name: Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to build and upload, for example v0.0.2
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.event.release.tag_name || inputs.tag }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  macos:
    name: Build macOS App
    runs-on: macos-latest
    timeout-minutes: 30
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name || inputs.tag }}

    steps:
      - name: Checkout Release Tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - name: Configure Paths
        run: |
          echo "RELEASE_BUILD_DIR=$RUNNER_TEMP/typeclaw-release" >> "$GITHUB_ENV"
          echo "RELEASE_ZIP=$RUNNER_TEMP/typeclaw-release/dist/TypeClaw-macos-universal.zip" >> "$GITHUB_ENV"

      - name: Toolchain
        run: |
          rustup show
          cargo --version
          rustc --version
          swift --version
          xcodebuild -version

      - name: Verify Release Version
        run: ./scripts/verify-release-version.sh "$RELEASE_TAG"

      - name: Cache
        uses: Swatinem/rust-cache@v2.8.1
        with:
          shared-key: release-macos

      - name: Install Universal Rust Targets
        run: rustup target add aarch64-apple-darwin x86_64-apple-darwin

      - name: Test
        run: cargo test --workspace --locked

      - name: Build Ad-Hoc Universal App
        run: |
          BUILD_DIR="$RELEASE_BUILD_DIR" \
          CARGO_TARGET_DIR="$PWD/target" \
          CODESIGN_IDENTITY="-" \
          ./macos/scripts/package-universal.sh

      - name: Verify Artifact
        run: |
          test -f "$RELEASE_ZIP"
          file "$RELEASE_BUILD_DIR/TypeClaw.app/Contents/MacOS/TypeClaw"
          codesign --verify --strict --verbose=2 "$RELEASE_BUILD_DIR/TypeClaw.app"
          APP_VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$RELEASE_BUILD_DIR/TypeClaw.app/Contents/Info.plist")"
          BUNDLE_VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' "$RELEASE_BUILD_DIR/TypeClaw.app/Contents/Info.plist")"
          test "$APP_VERSION" = "${RELEASE_TAG#v}"
          test "$BUNDLE_VERSION" != "1"
          echo "$APP_VERSION"
          echo "$BUNDLE_VERSION"
          shasum -a 256 "$RELEASE_ZIP" | tee "$RELEASE_ZIP.sha256"

      - name: Upload Release Assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$RELEASE_TAG" \
            "$RELEASE_ZIP#TypeClaw-macos-universal.zip" \
            "$RELEASE_ZIP.sha256#TypeClaw-macos-universal.zip.sha256" \
            --clobber
